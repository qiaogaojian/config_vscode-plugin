export interface Imported {
    content: string;
    cleanURL: string;
    type: string;
}
interface Handler {
    type: string;
    match(url: string): any;
    handle(match: any): any;
}
export declare class RemixURLResolver {
    private previouslyHandled;
    constructor();
    /**
    * Handle an import statement based on github
    * @params root The root of the github import statement
    * @params filePath path of the file in github
    */
    handleGithubCall(root: string, filePath: string): Promise<string>;
    /**
    * Handle an import statement based on http
    * @params url The url of the import statement
    * @params cleanURL
    */
    handleHttp(url: string, _: string): Promise<any>;
    /**
    * Handle an import statement based on https
    * @params url The url of the import statement
    * @params cleanURL
    */
    handleHttps(url: string, _: string): Promise<any>;
    handleSwarm(url: string, cleanURL: string): void;
    /**
    * Handle an import statement based on IPFS
    * @params url The url of the IPFS import statement
    */
    handleIPFS(url: string): Promise<any>;
    getHandlers(): Handler[];
    resolve(filePath: string, customHandlers?: Handler[]): Promise<Imported>;
}
export {};
